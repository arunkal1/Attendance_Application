<div class="container">
  <h1 class="list-h1" >Students</h1>
  <table class="list-table table">
    <thead>
      <tr>
        <th scope"col">#</th>
        <th scope"col">Name</th>
        <th scope"col">Active</th>
        <th scope"col">Stream</th>
        <th scope"col">Group</th>
      </tr>
    </thead>
    <tbody>
      <% @students.each do |student| %>
        <tr>
          <th scope="row"><%= student.id %></th>
          <td><%= link_to "#{student.name.capitalize}", student %></td>
          <td><%= student.active.to_s.capitalize%></td>
          <td><%= @group.find(student.group_id).stream.capitalize%></td>
          <td><%= student.group_id %></td>
        </tr>
      <% end %>

      <br>
      <br>
    </tbody>
  </table>
  <div class="new_student">
    <%= link_to 'Create a new student', new_student_path, class: "btn btn-primary" %>
  </div>
</div>
