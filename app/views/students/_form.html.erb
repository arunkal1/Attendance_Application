<%= form_for student do |p| %>

<center>
  <% if student.errors.any? %>
  <div class="alert alert-danger message-info" role="alert">
    <h2 class="alert-heading"><%= pluralize(student.errors.count, 'error') %> To Fix </h2>
    <hr>
      <% student.errors.full_messages.each do |message| %>
      <ul>
        <li style="list-style: none;">
          <p class="mb-0 message" role="alert"> <%= message %> </p>
        </li>
      </ul>
    <% end %>
  </div>
  <% end %>
  <br>
  <center>
  <div class="form">
    <div class="field">
      <%= p.label :name %>
      <div class="nameInput">
        <%= p.text_field :name %>
      </div>
    </div>
    <br>
    <div class="field">
      <%= p.label :group_id %>
      <div class="select">
        <%= p.select(:group_id, @course_list) %>
      </div>
    </div>
    <div class="field">
      <%= p.label :active %>
      <%= p.check_box :active %>
    </div>
    <br>
    <div class="action">
      <%= p.submit "Submit" %>
    </div>
  </div>
</center>
<% end %>
